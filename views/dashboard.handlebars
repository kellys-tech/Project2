

<!-- Loop over each object in the projects array -->
{{#each expensesArray as |expense| }}
<div class="row mb-4 expense">
    <div class="col-md-5">
        <h2>      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
{{expense.name}}
        </h2>
        <p>
            <span for="img" aria-label="money">💲</span>
            <!-- Pass needed_funding value to the helper function -->
            <span class="dollar-amount">{{expense.amount}}</span>
            needed!
        </p>
        <p>Category:{{category.name}}</p>
    </div>
    <div class="col-md-7">
        <p>
            {{expense.description}}
        </p>
    </div>
</div>
{{/each}}

