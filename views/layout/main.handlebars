<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" id = "desktopViewport">
  <title>My Budget Buddy</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="homepage">
     <header class="display-flex justify-space-between align-center p-2">
      <h1>
        <a href="/">My Budget Buddy</a>
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/dashboard">dashboard</a> |
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
      </nav>
      <div class="p-her0-headline">
        <p>
          "Test content"
        </p>
      </div>
    </header>
  <main id="site-content">
    <div class="p-hero bg-skyBlue">
    </div>

      <div class="p0herop"
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer class="p-footer bg-midnightBlue">
    </footer>
  </div>
  </main>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

<script src="path/to/chartjs/dist/Chart.js"></script>

</html>
